export interface IRegistrationDeleteRequest {
    /**
     * The registration id of the item.
     */
    registrationId: string;
    /**
     * The sideKey of the mam channel for validation.
     */
    sideKey: string;
}
