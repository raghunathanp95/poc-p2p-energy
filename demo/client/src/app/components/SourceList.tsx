import { ISource } from "../../models/api/ISource";
import ListConfigure from "./ListConfigure";

/**
 * Component which manages a list of sources.
 */
class SourceList extends ListConfigure<ISource> {
}

export default SourceList;
