export interface GridLiveConsumerState {
    /**
     * Are the details expanded.
     */
    isExpanded: boolean;

    /**
     * Paid balance.
     */
    paidBalance: number;

    /**
     * Owed balance.
     */
    owedBalance: number;
}
